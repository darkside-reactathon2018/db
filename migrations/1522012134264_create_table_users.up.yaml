- args:
    sql: CREATE TABLE "users"("firstname" text NOT NULL, "lastname" text NOT NULL,
      "id" integer NOT NULL, "username" text NOT NULL, PRIMARY KEY ("id") )
  type: run_sql
- args:
    name: users
  type: add_existing_table_or_view
